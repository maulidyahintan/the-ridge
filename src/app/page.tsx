import { HeroSection, AboutSection, ExperiencesSection } from '@/components'

export default function Home() {
  return (
    <main>
      <HeroSection />
      <AboutSection />
      <ExperiencesSection />
    </main>
  )
}
